// import { Inngest } from "inngest";

// // Create a client to send and receive events
// export const inngest = new Inngest({ id: "ai-learning-management-system" });

// inngest/client.js
import { Inngest } from "inngest";
export const inngest = new Inngest({ id: "ai-learning-management-system" });
